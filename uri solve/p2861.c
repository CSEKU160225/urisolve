#include <stdio.h>
int main()
{
	char name[30];
	int a,b,i=0;
	scanf("%d",&a);
	if(a>=2 && a<=99)
    {
   for(i=0;i<=a;i++)
   {
       gets(name);

   }
for(i=0;i<a;i++)
   {
       printf("gzuz\n");

   }

    }

}
